const greetUser = name => console.log(`Hello ${name}, welcome to the session on JS modules`);

const generatePattern = () => console.log('**************');

export default greetUser;
export { generatePattern }
