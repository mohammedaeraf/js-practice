import { add, square, sqrt } from "./math-utils.mjs";

const sum = add(75, 15);
console.log(`Sum = ${sum}`);

const sq = square(6);
console.log(`square = ${sq}`);

const squareRoot = sqrt(100);
console.log(`squareRoot = ${squareRoot}`);
